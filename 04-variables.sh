#!/bin/bash

echo "Please enter your PIN number"
read -s PIN_NUMBER # PIN_NUMBER is variable and to get input from user we use read command. -s means no ne can see the password wile we are typing

echo "PIN entered is : $PIN_NUMBER"